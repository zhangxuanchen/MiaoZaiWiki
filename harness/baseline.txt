# 喵藏 · 渲染基线
#
# 这个值 = 默认档（idle / scale 2 / 冻结时间 100000）渲染结果的 FNV-1a 64 哈希。
# 改了绘制代码之后跑：  ./harness/build/fingerprint --baseline
#   · 一致  → 默认档逐位没动（新增功能没碰到老路径）
#   · 变了  → 默认档被改了，先确认这是不是你想要的
#
# 重建基线：  ./harness/build/fingerprint > harness/baseline.txt
#
# 说明：历史上有过一个 491335AD41886EBE，那个值是旧脚本（tailhash.swift）在
# 未知渲染参数下算的，参数已不可考、无法复现，所以这里另立基线。
# 指纹的价值在「自洽」（改动前后一致），不在等于某个特定数值。
29B2960CAB55A8D9
